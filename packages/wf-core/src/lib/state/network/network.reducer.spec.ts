describe('networkReducer', () => {
  describe('networkActions.restore', () => {
    it('set the provided slice state to the store', () => {});
  });
  describe('networkActions.applyAlteration', () => {
    it('applies the right side of the alteration to the existing network state', () => {});
    it('applies each removal', () => {});
    it('applies each addition', () => {});
    it('applies each change', () => {});
    it('applies removals and additions before changes', () => {});
  });
  describe('networkActions.rollBackAlteration', () => {});
});
