import { describe } from 'vitest';

describe('Posts Grid Reducer', () => {
  const setup = () => {
    return {};
  };

  describe('loadPost', () => {
    it('add a post to the store', () => {
      const ctx = setup();
    });
  });

  describe('focusPost', () => {
    it('updates the current focused post id', () => {
      const ctx = setup();
    });
  });
});
